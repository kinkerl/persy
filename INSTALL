installation of persy
=========================

You can get fresh debian packages on persy.digitalkultur.net but if you want to install from source, you can use the usual:

 ./configure
 make

# as root:
 make install
